# 02 任务卡与验收标准库

> 本节是全部分的核心方法：把任何任务写成**可验收**的任务卡，并为科学计算任务准备一套现成的判据库。

## 本节目标

1. 会用**任务卡六件套**描述任务；
2. 会把形容词式要求改写成**可执行断言**；
3. 能从"验收标准库"里为第 1–8 章的典型任务挑选判据；
4. 会用提示词七条清单把任务说清楚。

## 先修

- 第 9 章（Agent 循环与失败模式）、本章 01 节（AGENTS.md 与计划模式）。

## 2.1 任务卡六件套

| 要素 | 写什么 | 例 |
| ---- | ---- | ---- |
| 目标 | 一句话说清要达成什么 | 算出每个学生的平均分 |
| 约束 | 不许动什么、用什么环境 | 只新增 scores_fixed.py，不动 data/ |
| 验收标准 | **可执行**的判据 | 长度 8、形状 (8,)、与手算一致 |
| 证据 | 要它给出什么证明 | 运行输出 + 断言结果 |
| 复核 | 人要检查哪几点 | 归一化是否逐列、排名是否带姓名 |
| 提交 | 交付形态 | 文件 + 说明 + 会话记录 |

## 2.2 验收标准的写法：从形容词到断言

| 差的写法 | 好的写法 |
| ---- | ---- |
| 结果要正确 | 平均分长度必须是 8，且与手算 [83.33, 65.0, …] 一致 |
| 图画得好看点 | 图保存到 output/trend.png；x 轴为小时、y 轴为温度（摄氏度）；标题含"传感器温度" |
| 检查有没有问题 | 用 assert 检查：行数守恒、缺失值策略已记录、归一化逐列 |
| 模型效果要好 | 5 折交叉验证最低分 ≥ 0.90，且预处理在 Pipeline 内 |

**为什么必须可执行**：只有可执行的判据才能被反复验证。同一段代码，两种 axis，只有一种能通过断言：

```python
import numpy as np
np.set_printoptions(precision=2, suppress=True)
scores = np.array([[82, 90, 78], [60, 65, 70], [95, 88, 92], [70, 72, 68]], dtype=float)

def check_student_mean(m):
    m = np.asarray(m)
    assert m.shape == (4,), "形状必须是 (4,)，每个学生一个平均分"
    assert np.allclose(m, scores.mean(axis=1)), "数值必须与手算一致"
    return True

for axis in (0, 1):
    try:
        check_student_mean(scores.mean(axis=axis))
        print("axis=%d -> 断言通过" % axis)
    except AssertionError as e:
        print("axis=%d -> 断言不通过：%s" % (axis, e))
```

```text
axis=0 -> 断言不通过：形状必须是 (4,)，每个学生一个平均分
axis=1 -> 断言通过
```

## 2.3 验收标准库（面向第 1–8 章）

写任务卡时，直接从下表挑判据，改成你的具体数值即可：

| 章节 / 任务类型 | 可执行判据（举例） |
| ---- | ---- |
| 第 1 章 NumPy | 形状与广播结果（`x.shape`）；与手算/解析解对照（`np.allclose`）；视图与拷贝（`np.shares_memory`）；残差与条件数（`np.linalg.norm(A@x-b)`、`np.linalg.cond(A)`） |
| 第 2 章 SymPy | 符号结果数值代入验证（`expr.subs`）；两式之差化简为 0（`simplify(a-b) == 0`）；与数值方法交叉验证 |
| 第 3 章 SciPy | 积分与解析解对照；优化收敛信息（`res.success`、`res.fun` 对比理论最优）；插值在节点处误差为 0；检验 p 值与手算一致 |
| 第 4 章 pandas | 行数守恒（清洗前后差值 = 删除行数）；缺失策略留痕；分组聚合与手算对照；表格一致性（`assert_frame_equal`） |
| 第 5 章 Matplotlib | 图保存路径与 dpi；坐标轴标签与单位；中文无乱码；随机种子固定；数据点数量与源数据一致 |
| 第 6 章 NetworkX | 节点/边数与源数据一致；连通性（`nx.is_connected`）；最短路长度与手算对照；社区划分的模块度 |
| 第 7 章 Statsmodels | 系数符号与量级合理；p 值与置信区间；R² 与残差诊断（Durbin-Watson）；重跑一致（固定种子） |
| 第 8 章 sklearn | 交叉验证分数区间；Pipeline 结构检查（防泄漏）；与基线对照（`DummyClassifier`）；指标口径与混淆矩阵一致 |
| 通用工程判据 | 脚本可重复运行；输出文件存在且非空；断言全部通过；`git diff` 只涉及预期文件 |

> 用法：把"第 8 章那类任务"的判据**数字化**——例如"5 折最低分 ≥ 0.90""行数守恒：11 → 10，删 1 行""max(axis=0) 全为 1"。

## 2.4 提示词七条清单

1. **说清目标与判据**，不要只说"帮我看看"；
2. **给最小复现**：能跑的 20 行，胜过 2000 行项目；
3. **限定范围**：明确"只改 X、不动 Y"；
4. **要求自证**：让它跑一次并贴输出，而不是"我觉得对"；
5. **要求打印中间量**：形状、量纲、前几行、统计量；
6. **一次只做一件事**：多个目标拆成多轮；
7. **不确定就问**：明确告诉它"信息不足时先提问，不要猜"。

## 2.5 一个完整示例

```text
【目标】修复 data/scores_buggy.py 中"每个学生平均分"和"归一化"两处错误，产出 data/scores_fixed.py。
【约束】只新增 scores_fixed.py，不修改 data/scores.csv 与 scores_buggy.py；使用现有 scicomp 环境。
【验收标准】
  1) student_mean 的形状是 (8,)；
  2) 归一化逐列进行：norm.min(axis=0) 全为 0、norm.max(axis=0) 全为 1；
  3) 加权总分第一名是王五（91.4 分），且打印时带姓名。
【证据】贴出 python data/scores_fixed.py 的完整输出，并给出三条 assert 的通过结果。
【复核】我会检查：是否动了原始数据、归一化是否逐列、排名是否真的排序。
【提交】scores_fixed.py + 运行输出 + 本次会话记录。
```

## 本节小结

1. 任务卡六件套：目标、约束、验收标准、证据、复核、提交；
2. 验收标准必须可执行，否则等于没有；
3. 判据库把第 1–8 章的知识变成了"AI 交付物的检查清单"；
4. 提示词七条清单解决"说不清"的问题。

## 思考题

1. 把"报告要有说服力"改写成两条可执行判据；
2. 第 7 章的回归任务里，哪三条判据可以防住"看起来显著其实不可靠"？
3. 为什么"要求它贴出运行输出"比"要求它保证正确"更有效？

## 练习与延伸阅读

- 练习：第 11 章 `exercises/` 第 7–8 题（任务卡与判据改写）；
- 下一节：[03-子代理、技能与工具.md](./03-子代理、技能与工具.md)。
